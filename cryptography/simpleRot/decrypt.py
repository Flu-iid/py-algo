# get len
# remove time
# check both values, rot them, if only one has meaning delete the other
# elif both have meaning return as an stringified 2-tuple
# if both dont have value return 1-tuple holding ?
import encrypt
import time
import math

float_a = 12.0
float_b = time.time()
float_c = math.pi

a = encrypt.ascii_encrypt("hello")

print(int(float_c))
